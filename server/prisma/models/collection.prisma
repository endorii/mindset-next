model Collection {
    id         String     @id @default(cuid())
    name       String
    path       String     @unique
    banner     String
    categories Category[]
    createdAt  DateTime   @default(now()) @map("created_at")
}
